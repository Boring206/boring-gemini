# Smithery Configuration for Boring MCP Server
# https://smithery.ai/docs/build/project-config

runtime: "python"
# Explicitly use pip to avoid mis-detection if other config files exist
packageManager: "pip"
installCommand: "pip install -e .[mcp-lite]"
entrypoint: "python -m boring.mcp.server"

